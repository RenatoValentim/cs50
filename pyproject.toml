[project]
name = "cs50"
version = "0.1.0"
description = "CS50 studies"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pytest>=9.1.1",
]

[dependency-groups]
dev = [
    "pytest-watcher>=0.6.3",
]
